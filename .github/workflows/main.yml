name: DevOps Actions

on:
  push:
    branches:
      - devops-actions-0

jobs:
  job0:
    name: Job0
    runs-on: self-hosted

    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4
        
      # 7. (Optional) Print build artifacts
      - name: Ping
        run: |
          echo "Branch : {$GITHUB_REF_NAME}"
          cd dorobe
          ls
          # docker build -t deveshmodi662/{$GITHUB_REF_NAME}-dorobe-app:v3 .
